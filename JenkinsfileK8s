pipeline {
  agent any
  stages {
    stage('Parse Input File') {
      steps {
        script {
          DOMAINS = parseDomains(readJSON(file: 'config.json'))
          echo "domains: ${DOMAINS}.domains"
          for (domain in DOMAINS) {
            sh 'echo $domain'
          }
        }
      }
    }
  }
}
