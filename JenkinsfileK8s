pipeline {
  agent any
  stages {
    stage('Parse Input File') {
      steps {
        script {
          DOMAINS = readJSON file:'domains.json'
          echo "domains: ${DOMAINS}.domains"
         }
      }
    }
  }
}
