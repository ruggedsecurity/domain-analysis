pipeline {
  agent any
  stages {
    stage('Parse Input File') {
      steps {
        script {
          DOMAINS = parseDomains(readJSON file:'domains.json')
          echo "domains: ${DOMAINS}.domains"
          for (domain in DOMAINS) {
            sh 'echo $domain'
          }
        }
      }
    }
  }
}
